/* let title = document.querySelector('.header-main');

window.addEventListener('scroll', () => {
    if(window.scrollY > 70) {
        title.style.cssText = 'position:fixed; top:10px';
    }else {
        // title.style.cssText = 'visibility:hidden;';

    }
}); */

